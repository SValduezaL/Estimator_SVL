# Attachment stress PDFs

Generated locally (not committed). From the `estimator/` directory:

```bash
uv run python -m evals.stress.fixtures.build_pdfs
```

Use `--force` to overwrite existing files.
